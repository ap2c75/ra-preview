{
  "name": "somun-chatbot-api",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply somun-chatbot --local",
    "db:remote": "wrangler d1 migrations apply somun-chatbot --remote"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
